<!DOCTYPE html>
<html>
    <head>
        <title>Record time</title>
    </head>

    <body>
        Who are you?
        
        <form method="post">
            <input type="hidden" name="qrcode" value="{{ $.QRCode }}" />
            {{ range $tm := .Teams }}
            <div>
                <input type="radio" name="team" value="{{ $tm }}" id="radio-{{ $tm }}" />
                <label for="radio-{{ $tm }}"> {{ $tm }} </label>
            </div>
            {{ end }}
            <input type="submit" formaction="/record-time" value="found it!" />
        </form>
    </body>

</html> 